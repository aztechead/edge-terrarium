apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: edge-terrarium
  labels:
    app: vault
    project: edge-terrarium
spec:
  selector:
    app: vault
  ports:
  - port: 8200
    targetPort: 8200
    protocol: TCP
    name: http
  type: ClusterIP