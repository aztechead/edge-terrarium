apiVersion: v1
kind: Service
metadata:
  name: file-storage
  namespace: edge-terrarium
  labels:
    app: file-storage
    project: edge-terrarium
spec:
  selector:
    app: file-storage
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: http
  type: ClusterIP